apiVersion: v1
kind: Service
metadata:
  name: bluegreen-service

spec:
  type: NodePort

  selector:
    app: myapp
    version: blue

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30008